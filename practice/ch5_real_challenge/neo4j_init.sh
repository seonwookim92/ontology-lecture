#!/bin/bash
# =============================================================================
# neo4j_init.sh — Neo4j 초기화 스크립트 (ch5)
#
# ch5는 덤프 파일 없이 빈 DB로 시작합니다.
# 데이터는 n8n 워크플로우(01~04_load_*.json)를 통해 적재합니다.
# =============================================================================
set -e

exec /startup/docker-entrypoint.sh neo4j
