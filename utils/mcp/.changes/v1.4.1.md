## v1.4.1 - 2026-02-20
### Patch
* Allow unauthenticated "ping" requests. Enable with the `NEO4J_HTTP_ALLOW_UNAUTHENTICATED_PING` environment
variable or the `--neo4j-http-allow-unauthenticated-ping` CLI flag.

* Fix missing version string in HTTP metrics.
* Fixed the type in argSlice preventing neo4j-http-auth-header-name from being parsed as expected.
